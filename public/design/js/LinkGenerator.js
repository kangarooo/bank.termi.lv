var LinkGenerator = new Class({
//	Binds: [],
	Implements: [Options, Events],
	options: {
	},
	initialize: function(options){
		this.setOptions(this.options, options);
	}
	, setActive: function(s){
		this.active(s);
	}
	, setActivePart: function(s, part){
		
	}
});